
*.pdf

soa_aso_comparator discarded for now.c
# Build artifacts (gcc/mingw)
*.exe
*.o
*.obj
*.out
*.pdb
*.ilk
*.exp
*.lib

# OS/editor noise
.DS_Store
Thumbs.db
*.swp

# Local run config (per-machine, not checked in)
*.ini

# Compilation output (every Makefile builds into builds/ at repo root)
/builds/

# Claude Code local tool config
.claude/

# Per-machine editor config (compilerPath/intelliSenseMode differ
# between the Windows and Linux dev machines) — never checked in
.vscode/

# Poor human agents chat (see dbj_poor_mans_message_bus.md) — the folder and
# the roster are tracked, the talk in it is not
/.colocuting/*
!/.colocuting/.gitkeep
!/.colocuting/colocutor_names.json

# Written by dbj_the_game/make.sh before every build. Generated, so not
# committed -- milestone_iteration.inc beside it is the hand-written one.
/dbj_the_game/build_timestamp.inc

# dbj_the_game/lineage/ is NOT ignored: the C++ ancestor is committed here on
# purpose, so the reference survives a lost drive. Upstream MIT
# (© Pedro Foresti Leão), used with the author's written permission.
