// Where this build sits on the ladder in docs/milestones.md.
//
// Bumped by hand. Nothing generates this file and nothing derives these two
// numbers -- that is the point: the build states its own position, and the
// statement is wrong until a human corrects it.
//
// Unrelated to the `version:` keys in docs/. Those version documents; these
// version the program.
// Human supervisor updates these values
#define DBJ_THE_GAME_MILESTONE 1
#define DBJ_THE_GAME_ITERATION 13
